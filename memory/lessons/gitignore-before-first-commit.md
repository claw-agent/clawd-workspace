# Set up .gitignore BEFORE first commit
**Date:** 2026-02-09
**Lesson:** Committed a 136MB Chrome binary to git because .gitignore wasn't set up yet. Required `git rm -r --cached` to fix. Always set up .gitignore before the first commit.
